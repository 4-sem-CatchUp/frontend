import React from "react";
import ProfileCard from "./ProfileCard";

export default function ProfileComments() {
  return (
    <ProfileCard title="Comments">
      Comment section where people can see previous comments made for the profile, and and input where they can add their own comment.
    </ProfileCard>
  );
}
